Bootstrap: docker
From: debian:bullseye-slim

%post
  apt-get update && apt-get install -y cpufrequtils curl

%files
	cpufreq-influxdb.sh /usr/local/bin/

%post
    chmod +x /usr/local/bin/cpufreq-influxdb.sh

%startscript
    /usr/local/bin/cpufreq-influxdb.sh

%runscript
    /usr/local/bin/cpufreq-influxdb.sh